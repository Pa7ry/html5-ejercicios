n=5
function largestNumber(n) {
    var supernumero = new Array(n); 
    for (var i = 0; i <n; i++); {
        supernumero [i] = 9;}
    var numeroTotal = supernumero.join();
    return numeroTotal;
}
